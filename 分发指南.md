# Video AI - 分发指南

## 📦 给朋友使用的打包方案

### 🥇 推荐：静态文件打包（最简单）

#### Windows 用户：
```bash
# 双击运行
build.bat
```

#### Mac/Linux 用户：
```bash
# 运行脚本
chmod +x build.sh
./build.sh
```

#### 将会得到：
- ✅ `video-ai-v1.0.0.zip`（< 1 MB）

---

## 🚀 朋友如何使用

### 方式 1：使用启动脚本（推荐）⭐

#### Windows 用户：
1. 解压 `video-ai-v1.0.0.zip`
2. 进入解压后的文件夹
3. 双击运行 `start-server.bat`
4. 等待服务器启动（显示 "Accepting connections"）
5. 浏览器会自动打开 http://localhost:3000
6. 点击右上角「设置密钥」按钮
7. 输入从 allapi.store 获取的 API key
8. 开始使用！

#### Mac/Linux 用户：
1. 解压 `video-ai-v1.0.0.zip`
2. 打开终端，进入解压后的文件夹
3. 运行：`chmod +x start-server.sh && ./start-server.sh`
4. 浏览器访问：http://localhost:3000
5. 点击右上角「设置密钥」按钮
6. 输入从 allapi.store 获取的 API key
7. 开始使用！

### 方式 2：手动启动服务器

如果启动脚本无法运行，可以手动启动：

```bash
# 进入 dist 目录
cd dist

# 使用 Node.js（推荐，最快）
npx serve -p 3000

# 或使用 Python（如果已安装）
python -m http.server 3000    # Windows
python3 -m http.server 3000   # Mac/Linux

# 然后在浏览器访问
# http://localhost:3000
```

### ⚠️ 重要提示

**不能直接双击 index.html**
- 由于浏览器安全限制（CORS），ES6 模块无法通过 `file://` 协议加载
- 必须通过 HTTP/HTTPS 服务器访问
- 推荐使用启动脚本，一键启动本地服务器

---

## 🔐 关于 API Key 的安全性

### ✅ 当前设计已经安全

```typescript
// API Key 存储在用户的浏览器 localStorage
// 每个用户必须自己输入，不会共享
const apiKey = localStorage.getItem('videoai_api_key');
```

**这意味着：**
- ✅ 你的代码不包含任何 API key
- ✅ 每个朋友需要自己注册 allapi.store
- ✅ 每个 API key 独立存储在各自的浏览器中
- ✅ 完全不用担心 key 泄露

---

## 📊 方案对比

| 维度 | 静态打包（推荐） | Electron 桌面端 | PWA |
|------|------------------|----------------|-----|
| **文件大小** | < 1 MB ✅ | 100+ MB ❌ | < 5 MB ✅ |
| **打包时间** | 3 秒 ✅ | 30+ 秒 ❌ | 10 秒 |
| **安装方式** | 解压即用 ✅ | 需要安装 | 一键安装 ✅ |
| **API Key 安全** | localStorage ✅ | localStorage ✅ | localStorage ✅ |
| **跨平台** | 全平台 ✅ | 需分别打包 ❌ | 全平台 ✅ |
| **开发维护** | 简单 ✅ | 复杂 ❌ | 中等 |
| **更新方式** | 替换文件 ✅ | 重装安装包 ❌ | 自动更新 |

---

## 🎯 为什么不推荐 Electron？

### Electron 的劣势：
1. ❌ **文件巨大**: 增加约 100-200 MB
2. ❌ **打包复杂**: 需要单独构建 Windows/Mac/Linux 版本
3. ❌ **更新麻烦**: 用户需要下载新安装包
4. ❌ **开发成本高**: 需要学习 Electron API
5. ❌ **无实际优势**: 你的 API key 处理不需要 Electron

### Electron 的优势（对你无效）：
- ✅ 原生窗口控制 → Web 也可以全屏
- ✅ 系统托盘 → 不需要
- ✅ 文件系统访问 → 不需要（纯在线工具）
- ✅ 本地数据库 → 已经用 localStorage

---

## 🌍 部署选项（如果需要）

### 免费平台（个人使用）
| 平台 | 地址 | 特点 |
|------|------|------|
| **Vercel** | vercel.com | 全球 CDN，自动 HTTPS，连接 GitHub 自动部署 |
| **Netlify** | netlify.com | 拖放部署，免费额度高 |
| **Cloudflare Pages** | pages.dev | 极速 CDN，免费 SSL |
| **GitHub Pages** | pages.github.com | 免费，但无 CI/CD |

### 部署步骤（Vercel 示例）：
```bash
# 1. 推送到 GitHub
git init
git add .
git commit -m "Release v1.0.0"
git branch -M main
git remote add origin https://github.com/yourname/video-ai.git
git push -u origin main

# 2. 连接 Vercel（5 分钟）
# - 访问 vercel.com
# - 导入 GitHub 仓库
# - 自动完成！
```

---

## 💡 总结建议

### 你的需求分析：
- ✅ 分享给朋友使用 → **静态打包最合适**
- ✅ 不想共享 API key → **已实现（localStorage）**
- ✅ 最早想做桌面端 → **桌面端没有优势**

### 最终建议：**使用静态文件打包**

**优势：**
1. 📦 文件小（< 1 MB）
2. 🚀 部署快（一键打包）
3. 💰 零成本（无需任何服务）
4. 🔒 API 安全（每个用户独立 key）
5. 🌍 跨平台（所有设备都能用）

---

## 📝 技术说明

### 当前项目架构：
```
纯前端应用
├─ React + TypeScript（UI）
├─ Vite（构建工具）
├─ Tailwind CSS（样式）
└─ localStorage（数据持久化）

API 调用：
├─ 直接从浏览器调用 allapi.store API
└─ 无需后端服务器
```

### 这是什么架构？
✅ **纯静态 SPA（单页应用）**
✅ **Client-side Rendering**
✅ **无服务端逻辑**

### 为什么不需要 Electron？
- 你的应用没有需要桌面 API 的功能
- 所有 API 调用都是浏览器可以做的
- Electron 只会增加不必要的复杂度和体积

---

## 🎁 给朋友的使用说明（可复制）

```
Video AI 使用说明

一、启动应用

Windows 用户：
  1. 解压下载的 ZIP 文件
  2. 双击运行 start-server.bat
  3. 等待服务器启动
  4. 浏览器会自动打开应用

Mac/Linux 用户：
  1. 解压下载的 ZIP 文件
  2. 打开终端，进入解压目录
  3. 运行：chmod +x start-server.sh && ./start-server.sh
  4. 浏览器访问：http://localhost:3000

二、设置 API 密钥
  1. 点击右上角「设置密钥」按钮
  2. 从 allapi.store 获取 API 密钥（需要注册账号）
  3. 输入密钥并保存

三、开始生成视频
  1. 选择视频模型（Veo 或 Sora）
  2. 输入提示词或使用预设模板
  3. 点击「生成视频」按钮
  4. 等待生成完成（通常 30 秒 - 5 分钟）
  5. 在历史记录中查看和下载视频

四、注意事项
  - 首次使用前请确保已安装 Node.js（启动脚本会自动检测）
  - API 密钥存储在您的浏览器本地，不会泄露
  - 每个用户需要单独获取 API 密钥
  - 建议使用 Chrome 或 Edge 浏览器以获得最佳体验
  - 按 Ctrl+C 可停止服务器
  - 免费用户可以通过 allapi.store 免费获取一定额度的 API 调用

技术支持：
  - 如有问题，请联系开发者
```
